'use strict';

module.exports = {
    getChartData(email, password) {
        //check sql
        return {x:['a', 'b', 'c'], y:[1, 2, 3]};
    },
    addCrime(payload) {
        //check sql
        return 'OK';
    },
    searchCrimes(payload) {
        //check sql
        return 'OK';
    }
};
